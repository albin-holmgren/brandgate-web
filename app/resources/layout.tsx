export const metadata = {
  title: "Resources - BrandGate",
  description: "Blog, guides, tools, and support resources for B2B wholesale.",
};

export default function ResourcesLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
